# A4.26 Runtime Ambiguity Boundary Audit — 2026-07-04

Status:
Read-only audit (planning/diagnostic)

Current runtime classification:
`CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS`

A4 posture (separate):
`A4_STABILITY_PROOF_OBSERVED`

A4:
Unresolved

Live readiness:
Not claimed

OR-20260630-008:
Remains **not_promoted**

---

## Canonical Ori Vault Check

| Path | Result |
|------|--------|
| `C:\Users\nalle\OneDrive\Documents\Obsidian\Trackta OS\Ori` | **Not found** |
| `.\Ori` (workspace-local) | **Found** |

Audit written to workspace-local Ori. Non-destructive merge recommended when canonical vault appears.

---

## 1. Files Inspected (read-only)

| File | Role |
|------|------|
| `dashboard_server.js` | `/api/runtime-health` route (`app.get`, ~4872); `buildVulcanRuntimeHealth` (~4811) |
| `runtime_health.js` | `classifyRuntimeHealth` (~544); decision tree (~619–656); `supportsSoakClaim` (~662–665); `supportsLiveReadiness` hard-false (~689) |
| `runtime_evidence.js` | `collectRuntimeEvidence`, `buildEvidenceShape`, `readLockEvidence`, `readHeartbeatEvidence`, scanner read |
| `test_runtime_health.js` | Classification fixtures (HEALTHY_DRY_RUN, observation, soak vs live invariants) |
| Stage 7 re-verification response (2026-07-04) | Live `/api/runtime-health` fields used as evidence baseline |

**Code changed:** **No**

---

## 2. Classifier Path Found

```
GET /api/runtime-health
  → dashboard_server.js: buildVulcanRuntimeHealth()
    → runtime_evidence.collectRuntimeEvidence({ auditTailLimit: 50 })
    → runtime_health.classifyRuntimeHealth(evidence)
      → classifyA4Health(evidence.a4Evidence)   // additive warnings only; does NOT change classification
    → sanitizeA4HealthForDisplay(result.a4Health)
  → JSON response (HTTP 200 even on internal errors)
```

Top-level classification is decided **before** A4 health is appended. A4 warnings (`A4_STABILITY_PROOF_OBSERVED`) appear in `warnings[]` but do not upgrade or downgrade the top-level `classification`.

---

## 3. Exact Reason Runtime Remains Ambiguous

The decision tree in `classifyRuntimeHealth` reaches `CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS` at **branch 6**:

```javascript
// 6. Capital is safe but runtime attribution is incomplete.
else if (exposure === "none") {
  classification = CLASSIFICATIONS.CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS;
}
```

This branch fires when **all** of the following hold:

| Prior branch | Required to match | Current Stage 7 state | Match? |
|--------------|-------------------|----------------------|--------|
| 1 | `exposure !== "active"/"possible"` | `capitalExposure: "none"` | ✓ (falls through) |
| 2 | `stoppedConfirmed !== true` | not stopped | ✓ |
| 3 | `hasAudit && monitorDriven && !executorLoopConfirmed` | `monitorDriven: false` | ✗ (skipped) |
| 4 | `executorLoopConfirmed === true` | `executorLoopConfirmed: false` | ✗ (skipped) |
| 5 | `observationOnly === true && exposure === "none" && warnings.length === 0` | `observationOnly` **not supplied**; `warnings.length === 4` | ✗ (skipped) |
| **6** | `exposure === "none"` | true | **→ AMBIGUOUS** |

Summary string (by design): *"No capital exposure, but producer/loop/lock/scanner evidence is incomplete."*

**HTTP 200 reachability is unrelated to classification.** The endpoint always returns 200 with a conservative verdict; ambiguity reflects **evidence quality**, not route failure.

---

## 4. Active Warnings and Failing Signals (Stage 7 evidence)

| Warning | Root signal | Current value | Source |
|---------|-------------|---------------|--------|
| `STALE_SCANNER` | `scanner.state === STALE` | `scannerFreshness: "stale"`, age ~521M ms (~6 days) | `scanner_health.json` last updated 2026-06-28 |
| `HEARTBEAT_MISSING_WARNING` | `heartbeat.state === MISSING` | `heartbeatState: "heartbeat_missing"` | `executor_singleton.lock.json` **absent** → heartbeat inferred missing |
| `EXECUTOR_LOOP_UNCONFIRMED` | `!(lock PRESENT_CURRENT && heartbeat CURRENT)` | `lockState: "lock_unknown"`, loop not confirmed | No lock file; `expectExecutorLoop` intentionally unset in dashboard |
| `A4_STABILITY_PROOF_OBSERVED` | A4 additive warning | `a4Health.status` | A4 stability visible; **does not affect top-level classification** |

Supporting uncertainties from collection meta:
- `pipelineMode: unknown` (no lock file → mode unknown)
- `liveArmed: unknown` (no lock file → not explicitly false)

---

## 5. Conditions for Readiness / Promotion-Safe Classifications

### `supportsLiveReadiness: true`

**Never emitted.** Hard-coded `false` in:
- `runtime_health.js` line ~689 (`classifyRuntimeHealth` return)
- `dashboard_server.js` line ~4849 (`buildVulcanRuntimeHealth` override)
- `classifyA4Health` line ~537–538 (A4 invariant)

No evidence path or OR promotion status can set this true today.

### `supportsSoakClaim: true`

Requires **both**:
1. `classification === HEALTHY_DRY_RUN` **or** `RUNNING_WITH_WARNINGS`
2. `executorLoopConfirmed === true`

`executorLoopConfirmed` requires:
- `lock.state === lock_present_current`
- `heartbeat.state === heartbeat_current`

`HEALTHY_DRY_RUN` additionally requires:
- `scanner.state === fresh` (< 5 min)
- `liveArmed === false`
- `warnings.length === 0`

Current state fails all of the above.

### Promotion-safe (less ambiguous) classifications

| Classification | Requirements | Current gap |
|----------------|--------------|-------------|
| `HEALTHY_OBSERVATION` | `observationOnly: true`, `exposure: none`, **zero warnings** | `observationOnly` not passed by `buildEvidenceShape`; 4 warnings active |
| `HEALTHY_DRY_RUN` | Confirmed loop + fresh scanner + `liveArmed: false` + zero warnings | No lock/loop; scanner stale |
| `RUNNING_WITH_WARNINGS` | Confirmed loop + degraded signals | No lock/loop |
| `MONITOR_DRIVEN_AUDIT_WITH_WARNINGS` | Audit + monitor-driven + no loop | `monitorDriven: false` (tail dominated by dashboard/live_executor status rows) |

---

## 6. Hypothesis Checklist (from audit brief)

| Hypothesis | Verdict |
|------------|---------|
| Missing monitor heartbeat | **Partially** — heartbeat missing because lock absent; monitor PID 6568 running but not producing lock/heartbeat evidence |
| Stale monitor/scanner state | **Yes** — primary driver (`STALE_SCANNER`) |
| Dashboard/monitor PID mismatch | **No** — monitor 6568 running; not a PID conflict issue |
| Absent runtime provenance | **Partially** — lock absent → loop unconfirmed; not a schema bug |
| Schema mismatch | **No** — evidence shapes match classifier expectations |
| Missing A4 approval coupling | **No** — A4 approval is separate; does not gate top-level classification |
| Missing capital safety proof | **No** — `capitalExposure: "none"` is satisfied |
| Missing soak window proof | **Yes** — requires confirmed executor loop (absent) |
| OR-20260630-008 not_promoted blocks classification | **No** — zero code references to outcome records in classifier |
| Classifier intentionally refuse readiness | **Yes** — by design; `supportsLiveReadiness` always false |

---

## 7. Exact Missing Evidence for Next Gate

To move **off** `CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS` toward a less-ambiguous operational classification (still not live readiness):

**Minimum operational evidence gaps:**

1. **Fresh scanner** — `scanner_health.json` with `lastScanAt` within 5 minutes (or scanner process restarted).
2. **Executor loop confirmation** — `executor_singleton.lock.json` present with current `updatedAt` (requires `live_executor.js --loop` or equivalent producing lock refresh).
3. **Zero operational warnings** — only achievable after (1) and (2) resolve `STALE_SCANNER`, `HEARTBEAT_MISSING`, `EXECUTOR_LOOP_UNCONFIRMED`.

Optional for `HEALTHY_OBSERVATION` path:
4. **`observationOnly: true`** in evidence — not currently supplied by `buildEvidenceShape`; would require a future evidence-collector change if paper-monitor posture should classify differently.

**A4-specific note:** `A4_STABILITY_PROOF_OBSERVED` is visible and correct. Moving to `A4_VERIFIED_DEDICATED` is a **separate governance gate** (Taylor signs A4.24 decision + future approval coupling). It does **not** change top-level `CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS`.

---

## 8. Safety Confirmation

| Check | Result |
|-------|--------|
| `.env` opened | **No** |
| Secrets inspected | **No** |
| `process.env` dumped | **No** |
| Code modified | **No** |
| OR-20260630-008 promoted | **No** (remains not_promoted) |
| Live readiness claimed | **No** |
| A4/A1 resolved claimed | **No** |

---

## 9. Recommended Next Gate

**A4.27 Operational Runtime Evidence Gap Planning**

---

## Closing Principle

HTTP 200 means the health endpoint is reachable. Ambiguity means operational evidence is incomplete — not that A4 stability or approval is wrong.
