# A4.35 Runtime Verification After Approval Coupling — 2026-07-04

**Phase:** Phase 2 · Pillar: Vulcan · A4 Dedicated RPC
**Prerequisite:** A4.24 **APPROVED_BY_TAYLOR** (A4.34); A4.25 coupling implemented.
**Type:** Runtime verification (read-only serving + one human-run approval emit). No promotion, no live/soak/capital action.
**Canonical Ori vault:** `C:\Users\nalle\OneDrive\Documents\Obsidian\Trackta OS\Ori` **NOT FOUND** on this machine. Workspace-local `.\Ori` used.

## Objective

Verify the approved A4.24 human decision is represented in the **live** runtime evidence path via the A4.25 approval-coupling mechanism, without promoting OR-20260630-008 or claiming live/soak/capital readiness.

## Sequence Executed

1. Inspected A4.25 implementation note + `a4_approval.js`, `a4_approval_emit.js`, `runtime_evidence.js`, `runtime_health.js`, `dashboard_server.js`.
2. **Pre-emit read-only evidence** (`collectRuntimeEvidence` + `classifyA4Health`, same path as the endpoint):
   - `statusHint: A4_READ_ONLY_RPC_VERIFIED`; provider `helius_rpc_url_configured`; class `dedicated`
   - `proofScan.available: true`; `proofStability.stabilityCandidate: true` (provider/class consistent)
   - `approval.present: false` → posture `A4_STABILITY_PROOF_OBSERVED`
   - **Expected evidenceRef = `helius_rpc_url_configured:dedicated`** (all non-approval guards already satisfied).
3. Emitted the human approval event (CLI defaults match expected evidenceRef).
4. Confirmed the written audit row (secret-safe, allowlisted payload only).
5. Restarted the dashboard (old pid 34068 → new pid 40392) so the live endpoint serves the A4.25 code, then queried `/api/runtime-health`.
6. Confirmed live A4 posture elevated to `A4_VERIFIED_DEDICATED`.
7. Confirmed invariants preserved.

## Approval Emit Command

```
node a4_approval_emit.js --approver Taylor --status approved --evidence-ref helius_rpc_url_configured:dedicated --scope-note "A4.24 stability-evidence-only; not live/soak/capital"
```

Emit result: `ok: true`, `producer: a4_approval`, `eventType: A4_VERIFIED_DEDICATED_APPROVAL`, `evidenceRef: helius_rpc_url_configured:dedicated`, `capitalExposure: none`.

## Emitted Audit Row (confirmed)

| Field | Value |
|-------|-------|
| eventType | `A4_VERIFIED_DEDICATED_APPROVAL` |
| producer | `a4_approval` |
| approvalStatus | `approved` |
| decisionRef | `DECISION-2026-07-04-A4-STABILITY-PROOF-ACCEPTED` |
| evidenceRef | `helius_rpc_url_configured:dedicated` |
| approvedAtIso | `2026-07-05T02:10:49.350Z` |
| runtimeMode / authorityMode | `observation_only` / `human_required` |
| capitalExposure | `none` |
| Secret material | **None** — payload allowlisted (approver, approvalStatus, decisionRef, evidenceRef, approvedAtIso, scopeNote, invocationContext); no endpoint/url/key/slot/wallet. |

## EvidenceRef / Provider / Endpoint / Proof-Class Consistency

| Item | Value | Consistent? |
|------|-------|-------------|
| Approval evidenceRef | `helius_rpc_url_configured:dedicated` | — |
| Live provider label | `helius_rpc_url_configured` | ✓ |
| Live endpoint class | `dedicated` | ✓ |
| Stability provider/class | `helius_rpc_url_configured` / `dedicated` | ✓ |
| Proof class | `READ_ONLY_RPC_OK` / `getSlot` / `dedicated` / `fresh` | ✓ |
| `approval.evidenceRefConsistent` (classifier) | `true` | ✓ |

## Live `/api/runtime-health` Result (post-emit, pid 40392)

`a4Health`:
- `status: A4_VERIFIED_DEDICATED` · `severity: info` · `blockerActive: false` · `confidence: high`
- `approval: { present: true, approved: true, status: approved, approver: Taylor, decisionRef: DECISION-2026-07-04-A4-STABILITY-PROOF-ACCEPTED, evidenceRef: helius_rpc_url_configured:dedicated, approvedAtIso: 2026-07-05T02:10:49.350Z, expiresAtIso: null, freshness: fresh }` (expired: **false**)
- `approvalScan: { available: true, limit: 5000, errorCode: null }`
- `proofScan.available: true`; `proofStability.stabilityCandidate: true`
- `recommendedAction: "A4 dedicated RPC verified by explicit human approval; not live readiness. A1/execution/capital gates remain."`
- `a4Health.supportsLiveReadiness: false`; `a4Health.supportsSoakClaim: false`

`a4Summary.operatorMessage`: "A4 dedicated RPC verified by explicit human approval. This is not live readiness, human soak authorization, or capital permission."

Overall runtime classification: `CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS` (branch-6 idle-safe; `STALE_SCANNER` / `HEARTBEAT_MISSING_WARNING` / `EXECUTOR_LOOP_UNCONFIRMED`). This is the separate operational posture and is unaffected by — and does not affect — the additive A4 verified-dedicated status.

No secret material present anywhere in the payload (display approval object omits `secretRisk`/`evidenceRefConsistent`; both were confirmed `false`/`true` respectively in the read-only classifier check).

## A4_VERIFIED_DEDICATED in live runtime-health: **YES**

All guards passed:
- `proofStability.stabilityCandidate === true` ✓
- `proofScan.available === true` ✓
- `approval.present && approval.approved && approval.evidenceRefConsistent` ✓
- approval not expired (`expiresAtIso: null`, `freshness: fresh`) ✓
- no `secretRisk` ✓
- no active blocker posture ✓

## Invariants Preserved

| Invariant | Value |
|-----------|-------|
| `supportsLiveReadiness` (a4 + top) | **false** |
| `supportsSoakClaim` (a4 + top) | **false** — machine classifier flag only; **not** human soak authorization |
| `capitalExposure` | **none** |
| OR-20260630-008 promotion_status | **not_promoted** (untouched) |
| Live readiness claimed | **No** |
| Human soak readiness claimed | **No** |

## Safety Confirmations

| Item | Value |
|------|-------|
| Promotion authorized | **No** |
| Live readiness claimed | **No** |
| Human soak readiness claimed | **No** |
| Capital exposure enabled | **No** |
| Code changed | **No** |
| Runtime processes started | Dashboard **restarted** (34068 → 40392, read-only serving); scanner (10216) + executor loop (30564) were already running (user-started, not started by this gate); one human-run approval emit event written |
| `.env` opened | **No** |
| Secrets inspected | **No** |
| `process.env` dumped | **No** |

## Determination

**A4_VERIFIED_DEDICATED_LIVE_VERIFIED** — the A4.25 approval-coupling path works end-to-end on the live runtime: an explicit, secret-safe, human-authored approval event elevates the additive A4 posture to `A4_VERIFIED_DEDICATED` while live readiness, human soak authorization, capital exposure, and OR promotion all remain unauthorized.

## Recommended Next Gate

**A4.36 Verified-Dedicated Governance Boundary Review**
