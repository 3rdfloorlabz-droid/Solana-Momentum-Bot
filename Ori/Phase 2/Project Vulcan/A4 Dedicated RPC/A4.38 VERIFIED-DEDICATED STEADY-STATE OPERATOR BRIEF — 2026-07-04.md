# A4.38 Verified-Dedicated Steady-State Operator Brief — 2026-07-04

Status:
Operator documentation only — no promotion, no runtime execution, no readiness claim

Audience:
Operators and gate runners working on TracktaOS/Ori A4 dedicated-RPC posture.

Live readiness:
Not claimed

Human soak readiness:
Not claimed

OR-20260630-008:
Remains **not_promoted** (unchanged)

**Code changed:** **No**

Full governance detail: **A4.36** · Manifest summary: **ACTIVE_MANIFEST.md** (Runtime health section, A4.37 backfill)

---

## 1. Current State Summary

| Item | Steady-state value |
|------|-------------------|
| A4.24 decision | **APPROVED_BY_TAYLOR** (stability-evidence-only) |
| A4 approval coupling | Implemented (A4.25), live-verified (A4.35) |
| A4 additive posture (`a4Health.status`) | **`A4_VERIFIED_DEDICATED`** when all guards hold at query time |
| Overall runtime classification | May be **`CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS`** (branch-6 idle-safe) — **independent** from A4 |
| `capitalExposure` | **`none`** |
| `supportsLiveReadiness` | **`false`** (hard-coded) |
| `supportsSoakClaim` | Machine flag only — **not** human soak authorization |
| OR-20260630-008 | **`not_promoted`** |
| A1 | **Unresolved** |

**Operator takeaway:** The dedicated-RPC **stability evidence path is verified and governance-visible**. That is **not** permission to trade, soak, expose capital, or promote OR-20260630-008.

---

## 2. Meaning of `A4_VERIFIED_DEDICATED`

At classification time, all of the following are true:

1. Taylor approved A4.24 (stability-evidence-only; A4.34 sign-off).
2. A human-run `A4_VERIFIED_DEDICATED_APPROVAL` audit event is present and approved.
3. Repeated safe read-only RPC proof stability evidence is visible (`stabilityCandidate: true`).
4. Targeted proof scan is available; approval `evidenceRef` matches current provider/endpoint class.
5. Approval is not expired; no secretRisk; no active A4 blocker posture.

**Verified is current, not permanent.** Revocation, expiry, evidence regression, provider/class change, or blocker reactivation can revert posture.

**Check live status:** `GET /api/runtime-health` → read `a4Health.status`, `a4Health.approval`, `a4Summary.operatorMessage`.

---

## 3. Non-Meanings / Forbidden Interpretations

Do **not** interpret `A4_VERIFIED_DEDICATED` as any of the following:

| Forbidden interpretation | Why |
|--------------------------|-----|
| Live readiness | `supportsLiveReadiness` stays **false** |
| Human soak authorization | `supportsSoakClaim` is a machine flag only |
| Capital exposure approval | `capitalExposure` stays **`none`** |
| Trading authorization | No execution/liveArmed implication |
| OR-20260630-008 promotion | Separate governance track; remains **not_promoted** |
| Continuous observation readiness | Idle-safe branch-6 is acceptable without scanner/executor |
| B2A/R7b observation result | Infrastructure verified ≠ observation window complete |
| A1 resolved | Signing/authority gates unchanged |
| Dashboard green = go live | Dashboard restart is hygiene, not readiness |

---

## 4. Idle-Safe Posture (Default Steady State)

**When:** No deliberate observation window is active.

| OK | Not required |
|----|--------------|
| Dashboard and/or monitor running | Scanner `--watch` continuously |
| Branch-6 `CAPITAL_SAFE_BUT_RUNTIME_AMBIGUOUS` | Executor `--loop` continuously |
| Lock absent or stale after confirmed stop | Preserving `HEALTHY_DRY_RUN` via always-on processes |
| `A4_VERIFIED_DEDICATED` + branch-6 combined | Treating branch-6 as a failure |

**Principle (A4.29):** Do not run scanner + executor continuously merely for dashboard cosmetics.

---

## 5. Active Observation Posture

**When:** A **future, explicitly authorized** B2A/R7b or operational evidence-collection gate says to run one.

| Requirement | Detail |
|-------------|--------|
| Mode | **`PIPELINE_DRY_RUN`** — preserve unless a future explicit capital gate says otherwise |
| Processes | `scanner_gmgn_trending.js --watch` + `live_executor.js --loop` (intentional window only) |
| Purpose | Deliberate paper-trading / dry-run evidence collection — **not** dashboard optics |
| Overall classification target | May reach `HEALTHY_DRY_RUN` during the window only |
| A4 axis | May remain `A4_VERIFIED_DEDICATED`; observation does not expand A4 meaning |

**Stop when the authorized window ends** (see §11).

---

## 6. Before Running Scanner/Executor Again

Even with `A4_VERIFIED_DEDICATED`, scanner/executor are **not** auto-authorized.

1. Confirm a **separate future gate** explicitly authorizes the observation window (not this brief).
2. Preflight: `PIPELINE_DRY_RUN` / `liveArmed: false` / `capitalExposure: none` via safe config checks (no `.env` inspection).
3. Use **`scanner_gmgn_trending.js`** only (legacy `scanner.js` does not write `scanner_health.json`).
4. Start only for the **declared window**; document start/stop in the gate receipt.
5. After start: verify `/api/runtime-health` (bounded curl + timeout).
6. After stop: expect branch-6 to return if idle; lock may orphan after `Stop-Process -Force` — self-heals ~3 min or remove after confirmed stop.

---

## 7. Before Any B2A/R7b Observation Window

1. Complete any **B2A/R7b planning/authorization gate** first — A4 verified-dedicated is **not** that authorization.
2. Confirm idle-safe baseline is acceptable; A4 status does not substitute for B2A/R7b gate criteria.
3. Run observation processes only for the authorized window (§5–§6).
4. Collect evidence via existing read-only tooling (`r7b_daily_summary.js`, etc.) per gate plan.
5. Stop processes at window end; record before/after `/api/runtime-health`.
6. Do **not** claim B2A/R7b complete from A4 status alone.

---

## 8. Before Any OR Promotion Consideration

1. OR-20260630-008 remains **`not_promoted`** — A4 verified-dedicated does **not** promote it.
2. Use a **dedicated OR promotion gate** with explicit Taylor sign-off.
3. Review OR outcome record separately from `a4Health.status`.
4. Do not change `promotion_status` without that gate.
5. A4 infrastructure verification is **necessary context**, not sufficient for OR promotion.

---

## 9. Before Any Live / Capital-Readiness Consideration

1. Confirm **`supportsLiveReadiness: false`** — it must remain false until a future explicit live-readiness gate (if ever authorized).
2. Confirm **`capitalExposure: none`**.
3. Confirm A1 and execution/signing/authority gates independently.
4. Do **not** disable `PIPELINE_DRY_RUN` or arm live execution based on A4 status.
5. Do **not** edit `.env` or inspect secrets as part of posture checks.
6. Use dedicated live-readiness / capital gates — not A4 verified-dedicated.

---

## 10. Operator Do / Don't

### Do

- Preserve **`PIPELINE_DRY_RUN`** unless a future explicit capital gate says otherwise.
- Treat **`A4_VERIFIED_DEDICATED`** as governance/runtime evidence only.
- Use **separate gates** for observation, promotion, and live-readiness decisions.
- Verify **`/api/runtime-health`** after any dashboard restart or approval/evidence change.
- Prefer read-only `collectRuntimeEvidence()` preflight if HTTP hangs on a stale dashboard (A4.35 lesson).
- Restart dashboard after code changes, then re-verify with bounded curl.
- Read **`a4Summary.operatorMessage`** and **`recommendedAction`** before acting.

### Don't

- Treat verified-dedicated as **trading authorization**.
- Run scanner/executor **continuously for dashboard cosmetics**.
- Claim **human soak readiness** from `supportsSoakClaim`.
- **Promote OR-20260630-008** without an explicit future promotion gate.
- Edit **`.env`** or inspect **secrets** for posture verification.
- Assume **`HEALTHY_DRY_RUN`** or branch-4 status means live-ready.
- Self-authorize approval events — use **`a4_approval_emit.js`** only under human governance gates.

---

## 11. Stop / Rollback (When Observation Is Later Authorized)

After any authorized observation window:

1. **Stop** `scanner_gmgn_trending.js --watch` and `live_executor.js --loop` (graceful preferred; force-stop is normal on Windows).
2. **Wait** ~3 min for lock TTL self-heal, or remove orphan lock **only after confirmed process stop**.
3. **Re-query** `/api/runtime-health` — expect return toward idle-safe branch-6 if scanner/executor stopped.
4. **Confirm** A4 additive status separately — may remain verified if approval/stability guards still hold.
5. **Do not** leave observation processes running indefinitely.
6. **Record** stop time and post-stop health snapshot in the gate receipt.

---

## 12. Model / Tool Routing

| Tool | Use for |
|------|---------|
| **Cursor / Composer** | Docs, manifests, small code changes, test runs, operator briefs |
| **Fable** | Multistep **dry-run execution gates only** (when explicitly authorized) |
| **Claude / Opus** | Governance judgment, architecture review, decision review |
| **ChatGPT** | Trajectory framing and prompt generation |

A4 steady-state checks and manifest/governance docs → **Cursor/Composer**. OR promotion, live-readiness, or capital decisions → **Claude/Opus** (human sign-off required). Observation execution windows → **Fable** only when a gate explicitly authorizes dry-run execution.

---

## 13. Quick Reference — Future Gate Types

| Need | Gate type (examples) | A4.38 does NOT authorize |
|------|----------------------|---------------------------|
| Re-check A4 after evidence change | Runtime verification gate | — |
| B2A/R7b observation window | Dedicated observation gate | Scanner/executor start |
| OR-20260630-008 promotion | OR promotion + Taylor sign-off | Promotion |
| Live / capital readiness | Dedicated live-readiness gate | Live trading, capital |
| Revoke A4 approval | Human governance + `a4_approval_emit.js` revoke status | — |

---

## 14. Safety Confirmation (This Gate)

| Check | Result |
|-------|--------|
| OR-20260630-008 promoted | **No** |
| Promotion authorized | **No** |
| Live readiness claimed | **No** |
| Human soak readiness claimed | **No** |
| Capital exposure enabled | **No** |
| Runtime processes started | **No** |
| `.env` opened | **No** |
| Secrets inspected | **No** |
| `process.env` dumped | **No** |
| Code changed | **No** |

---

## 15. Recommended Next Gate

**A4.39 Verified-Dedicated Periodic Re-Check Planning** — define when operators should re-verify A4 status (approval expiry, proof stale, provider change, post-restart) without implying observation or promotion.
